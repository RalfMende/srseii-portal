#!/bin/sh
set -eu

ROOT_INDEX="/www/index.html"
BACKUP="/www/index_luci_backup.html"

if [ ! -f "$BACKUP" ]; then
  echo "Backup not found: $BACKUP" >&2
  exit 1
fi

cp "$BACKUP" "$ROOT_INDEX"
echo "Root page restored from LuCI backup."
